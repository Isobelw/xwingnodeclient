box: node
build:
  steps:
    - npm-install              
deploy:
  steps:
    - joshholt/oracle-accs-deploy:
        opc_user: $OPC_USERNAME
        opc_password: $OPC_PASSWORD
        rest_url: $REST_URL
        domain: $IDENTITY_DOMAIN
        application_name: node_fighter
        application_type: java
        file: node_fighter-0.0.1.zip
